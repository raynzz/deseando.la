server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # No cachear el runtime config mientras probás
  location /config.js {
    add_header Cache-Control "no-store";
  }

  location / {
    try_files $uri /index.html;
  }
}
